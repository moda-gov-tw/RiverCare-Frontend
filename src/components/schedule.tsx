import { RiverStatus } from "@/interfaces/river.interface"

const Schedule = ({ gen, status }: { gen: number; status: RiverStatus }) => {
  return <div className="">Gen 0 ----＞ Gen 1</div>
}
export default Schedule
